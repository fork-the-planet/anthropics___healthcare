{
  "name": "@anthropic-ai/mcp-server-documents",
  "version": "0.0.1",
  "description": "MCP server for corpus contract Q&A with span-verified citations. Local, no network egress; speaks MCP over stdio.",
  "license": "MIT",
  "author": "Anthropic",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "bundle": "bun build src/index.ts --target=node --loader .sql:text --outfile=../documents.mjs"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
